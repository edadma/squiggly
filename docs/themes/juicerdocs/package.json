{
  "name": "juicerdocs",
  "version": "0.1.0",
  "private": true,
  "description": "Build pipeline for the juicerdocs theme's bundled CSS. Theme USERS don't run this — the compiled `static/juicerdocs.css` is committed. Theme AUTHORS regenerate it with `npm install && npm run build`.",
  "scripts": {
    "build": "tailwindcss -i src/tailwind.css -o static/juicerdocs.css --minify",
    "watch": "tailwindcss -i src/tailwind.css -o static/juicerdocs.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli":        "^4.0.0",
    "@tailwindcss/typography": "^0.5.16",
    "tailwindcss":             "^4.0.0"
  }
}
